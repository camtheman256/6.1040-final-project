User:
  $ref: "./User.yml"

Space:
  $ref: "./Space.yml"

Request:
  $ref: "./Request.yml"

Reply:
  $ref: "./Reply.yml"


gapiAuthPayload:
  type: object
  required:
    - token
  properties:
    token: 
      type: string

createSpacePayload:
  type: object
  required:
    - temp
  properties:
    temp:
      type: string

updateSpacePayload:
  type: object
  required:
    -temp
  properties:
    temp:
      type: string

createRequestPayload:
  type: object
  required:
    - temp
  properties:
    temp:
      type: string

updateRequestPayload:
  type: object
  required:
    -temp
  properties:
    temp:
      type: stringc

createReplyPayload:
  type: object
  required:
    - temp
  properties:
    temp:
      type: string

updateReplyPayload:
  type: object
  required:
    -temp
  properties:
    temp:
      type: string